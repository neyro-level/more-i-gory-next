# TASK 46.3 — single secret route

**Checked:** 2026-09-20 (Europe/Moscow)

Secret values were compared only in process and were not printed, logged or
written to the repository.

## Canonical route

```text
Secret Master more-i-gory-server/prod/MOREIGORY_DATABASE_URL
→ release/env renderer
→ /etc/moreigory/app.env:DATABASE_URI
→ Next.js + Payload runtime
→ cluster 4210557 / default_db
```

- `MOREIGORY_DATABASE_URL` exists and is the project-scoped source secret.
- Runtime code consumes only `DATABASE_URI`.
- The legacy generic `DATABASE_URL` currently exists with the same value but is
  not a second route and must not be consumed by project code. TASK 47 removes
  the duplicate during controlled credential/env hardening.
- Component secrets remain recovery/administration inputs; they are not an
  alternative application runtime URL.
- `MOREIGORY_STAGING_DATABASE_URL` does not exist and must not be created.

No Secret Master or server mutation was made by this task.
