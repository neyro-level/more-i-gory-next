# TASK 44.1 — target migration history inventory

Status: `PASS`

Checked: 2026-09-20 (Europe/Moscow)

## Guardrails

- All provider, server and database operations were read-only.
- No migration, DDL, seed, dump or production write was executed.
- No credential, database URL or row payload was printed or stored.
- A trailing carriage return in the server env file was stripped only in the
  querying process; the file itself was not changed.

## Repository chain

The PR !74 branch contains 26 TypeScript migration files plus `migrations/index.ts`.
Its exact pre-task head is `d9eb2e4df85d79d6085318656a86122299c46076`.

## Target matrix

| Contour | Resource identity | Consumer identity | `payload_migrations` | Verdict |
| --- | --- | --- | --- | --- |
| Timeweb managed PostgreSQL target | cluster `4210557`, `More-gory BD`, `postgres18`, status `started`, project `2932741` | database `default_db`, role `gen_user`, PostgreSQL `18.6` | table absent | `unmigrated` |
| Preview `more-previu.tw1.ru` | service `moreigory.service`, env file `/etc/moreigory/app.env` | resolves to the same `default_db` / `gen_user` target above | table absent | `unmigrated`; no separate preview history exists |
| Isolated staging / restore | planned by TASK 46.2 | none | resource does not exist | `resource_absent`; history was not simulated |
| Production runtime | production release not authorized | no separate deployed production consumer | no separate history | `not_deployed`; managed target remains `unmigrated` |

Timeweb read-only inventory returned exactly one database cluster for the project.
The server-side `psql` consumer check reported no table named
`public.payload_migrations`. Because the table is absent, no migration row data or
checksums were queried.

## Decision input for TASK 44.2

No known managed, preview or staging contour contains an applied Payload migration
history. Therefore this inventory provides no evidence that an old checksum from
the PR !74 historical files was applied. TASK 44.2 must still compare the exact
PR diff and test fixtures before choosing the historical repair path.

## Follow-up already covered by the plan

`/etc/moreigory/app.env` contains CRLF residue that breaks naive shell sourcing.
LF normalization belongs to TASK 47.2 and is not changed here.
