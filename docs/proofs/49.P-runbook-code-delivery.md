# TASK 49.P — runbook/code delivery

**Pull Request:** SourceCraft PR 94
**Risk:** RISKY
**Production:** out of scope

Delivered scope:

- portable pnpm standalone archive with exact manifest and SHA-256 sidecar;
- fail-closed immutable installer with safe extraction and atomic pointers;
- transactional artifact rollback with failed-smoke recovery;
- local five-minute systemd health monitoring and fixed log paths;
- executable Linux fixtures for package, install and rollback boundaries.

Local evidence on the final branch reached the production build after the full
test/check matrix. A duplicate builder process held the first Next.js build
lock; after terminating only that disposable process, the explicit staging-env
production build completed successfully with all 34 pages. The authoritative
delivery verdict is the one manual SourceCraft `merge-risky` run on the exact PR
head. Live preview mutation remains TASK 49.O after merge.
