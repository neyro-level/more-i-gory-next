# TASK 49.O — technical preview operational proof

Date: 2026-09-21  
Scope: `more-previu.tw1.ru` only; production domain was not entered.

## Runtime identity

- canonical `main`: `f06fdb06c1e42aff3a117c4b7a2f22b74455708d`;
- immutable release: `/opt/moreigory/releases/f06fdb06c1e42aff3a117c4b7a2f22b74455708d`;
- artifact SHA-256: `7c2be8c3f802d87c7c6d2645189cbc9fab3a403434d31282b22eeb7b92c28e41`;
- manifest: artifact v1, `next-standalone`, Node 24, exact main SHA;
- `current` points to `f06fdb0…`; `previous` points to the proven
  `f8de7ad…` release.

## Live acceptance

| Check | Result |
| --- | --- |
| Service, loopback health, Nginx config | PASS |
| `moreigory-healthcheck.timer` every five minutes | PASS |
| Admin and owner login against the single managed DB | PASS |
| Published property list/detail and draft 404 boundary | PASS |
| 29 linked public HTML routes | PASS, HTTP 200 |
| Preview robots boundary | PASS, `X-Robots-Tag: noindex, nofollow`; robots disallow |
| Sitemap and immutable static cache | PASS |
| S3 media credentials | PASS, Put → Head → Delete; no diagnostic object retained |
| Artifact rollback | PASS, `f06fdb0… → f8de7ad… → f06fdb0…` |

The first live rollback attempt exposed normal Node startup latency. The
transactional guard restored the original release automatically. Readiness
retry support was then added, covered by five Linux rollback fixtures, passed
the exact-head RISKY gate in SourceCraft run 135 and merged through PR 96.
The final bidirectional rollback passed without database down, restore or
schema changes.

## Access correction

The stored S3 pair was stale. The currently assigned bucket-user pair was read
through the project-scoped Timeweb API, validated with a disposable S3 object,
then synchronized to Secret Master and `/etc/moreigory/app.env`. Values were
not logged or committed. `moreigory-media` remained the only bucket and no new
database or storage service was created.

## Boundary

No request was sent to the production domain, DNS and production TLS were not
changed, and the final-domain release remains a separate explicit owner gate.
