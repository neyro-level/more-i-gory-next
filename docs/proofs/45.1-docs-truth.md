# TASK 45.1 — Docs truth

Дата: 2026-09-20.

## Canonical baseline

- Canonical repository: SourceCraft `integrator-p/more-i-gory-next`.
- Exact verified execution baseline before EPIC 45:
  `origin/main` `c824e9fa02ecfe370c859a851e3c0e0cedd48667`.
- Active program: `more-i-gory-production-readiness-2026-09`, Plan №3 v2 APPROVED.
- Production: not authorized; EPIC 55–56 require a separate owner release command.

## Reconciled facts

- EPIC 43, EPIC 51 and EPIC 44 are historical completed work, not the current focus.
- PostgreSQL 18 clean chain 26/26 and upgrade fixture passed in EPIC 44; the old historical-chain blocker was removed.
- Payload authentication/authorization, Payload Jobs and PostgreSQL backup/migration contracts are implemented and no longer marked `N/A`.
- External gates at the time of this v2 proof were consolidated in
  `OWNER_QUEUE.md`. The approved v3 plan later removed the separate staging DB
  and external CAPTCHA requirements; current canonical docs take precedence.
- The approved v2 plan and inventory are part of the EPIC 45 candidate branch;
  canonical status is established only by TASK 45.D merge evidence. The plan
  file is pinned to LF so its SHA-256 is stable across Windows and Linux
  checkouts.

## Verification

- Canonical docs contract tests: PASS, 5/5, command:
  `node --test scripts/final-conformance-closeout.test.mjs scripts/owner-queue.test.mjs scripts/owner-queue-forecast.test.mjs`.
- Approved inventory validation: PASS, 11/11 epics, 54 tasks.
- Stale current-state markers `842b0cce`, `NOW: EPIC 43` and the resolved historical migration blocker are absent from the active docs set.
- `git diff --check`: PASS.
