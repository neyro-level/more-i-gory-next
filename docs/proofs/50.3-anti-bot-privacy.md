# TASK 50.3 / 50.3A — anti-bot and privacy

Status: `SUPERSEDED_BY_V3_NO_EXTERNAL_CAPTCHA`

Checked: 2026-09-20 (Europe/Moscow)

## Historical access discovery

- Secret Master project `more-i-gory-server/prod`: no secret names matching
  Cloudflare, Turnstile or CAPTCHA.
- Canonical Secret Master project map: no dedicated Cloudflare project.
- Names-only cross-project search: no Cloudflare/Turnstile access found. A shared
  Yandex SmartCaptcha contour exists in another project and was not reused by
  analogy.
- Secret values were not printed or written to repository evidence.

No Cloudflare API mutation was attempted. The approved v3 plan then removed the
external CAPTCHA dependency completely.

## Current v3 implementation

- Turnstile code, widget, request token and environment keys are removed.
- Public leads require no external CAPTCHA account or credential.
- Server validation, honeypot, minimum-fill guard and trusted-client dual rate
  limit remain active.
- Public form renders a disabled state only while
  `NEXT_PUBLIC_LEADS_ENABLED=false`.
- PII/log/analytics and 100/300-day retention proofs are part of the task gate.

## Remaining proof

Run enabled/disabled E2E against the canonical local Payload intake. Keep
`NEXT_PUBLIC_LEADS_ENABLED=false` on failure. No external CAPTCHA owner action
or secret rotation is required.
