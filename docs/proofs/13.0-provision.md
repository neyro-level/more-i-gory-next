# TASK 13.0 — Host provision proof

**Дата:** 2026-09-19  
**Хост:** SSH alias `moreigory` (hostname `moregory1`)  
**Домен:** `more-previu.tw1.ru`  
**Секреты:** не включены. `DATABASE_URI` установлен из Secret Master `more-i-gory-server/prod` в `/etc/moreigory/app.env` (mode 640, owner root:deploy).

## Команды и статусы

| Проверка | Результат |
|---|---|
| `hostname` | PASS `moregory1` |
| `sudo -n true` | PASS |
| Node | PASS `v24.20.0` (`/usr/local/bin/node`) |
| pnpm | PASS `11.5.1` |
| Каталоги `/opt/moreigory/releases`, `/opt/moreigory/current` | PASS, `current` → bootstrap |
| systemd `moreigory` | PASS `active` + `enabled`, `Restart=always` |
| nginx vhost `more-previu.tw1.ru` (не default, не static example) | PASS |
| TLS Let's Encrypt `more-previu.tw1.ru` | PASS, expiry 2026-12-18 |
| `DATABASE_URI` в репозитории | PASS отсутствует |
| `DATABASE_URI` в `/etc/moreigory/app.env` | PASS (`grep -c` = 1) |
| `curl http://127.0.0.1:3000/api/health` | PASS 200 |
| `curl http://more-previu.tw1.ru/api/health` | PASS 200 |
| `curl https://more-previu.tw1.ru/api/health` | PASS 200 |

Runtime на этом шаге — bootstrap health (`ops/runtime/bootstrap-health.mjs`), не Next standalone. Immutable release — TASK 13.5.

## Не печаталось

IP, `DATABASE_URI`, содержимое `/etc/moreigory/app.env`, ключи Let's Encrypt.
