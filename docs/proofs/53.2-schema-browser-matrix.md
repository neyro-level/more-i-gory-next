# TASK 53.2 — schema and browser matrix

**Date:** 2026-09-21

**Preview:** `https://more-previu.tw1.ru`
**Production:** not entered

## Desktop browser

The home page plus `/podbor/`, the Crimea region scaffold, a draft analytics
article, the project-passport template and `/privacy/` were rendered in the
in-app Chromium browser.

For every representative page:

- one `h1` and one `main` were present;
- no horizontal overflow was detected;
- the owner preview navigation remained available;
- no invalid JSON-LD was rendered;
- no browser console error or warning was recorded.

The gated lead form rendered with `data-leads-enabled="false"`; no submission
was performed in this task.

## Mobile browser

The home page, `/podbor/`, the Crimea scaffold and a draft analytics article
were checked at a real CDP viewport of `390 × 844`:

- `documentElement.scrollWidth` stayed equal to `390`;
- every page retained one `h1`;
- the mobile menu summary was visible and opened its navigation;
- the opened menu exposed 36 links, including the complete preview section;
- following the primary `Контакты` link navigated to `/kontakty/` and rendered
  `Контакты «Море и Горы»`;
- the mobile console remained free of errors and warnings.

## Accessibility and 404

The `/podbor/` accessibility tree exposed 30 interactive nodes: 24 links, one
button, four textboxes and one checkbox. None had an empty accessible name.
The existing responsive/a11y contract also passed its width, keyboard, label,
CTA and reduced-motion assertions.

`/definitely-missing-preview-proof/` returned HTTP `404`, rendered the custom
heading `Такой страницы нет`, provided a home link, had no overflow and carried
both HTML and edge noindex protection.

## Structured-data boundary

No JSON-LD was rendered on the tested editorial scaffolds, legal page or
technical template. This is the expected fail-closed result because there is no
published factual passport after removal of the DB proof fixture.

The SEO contract test permits `RealEstateListing` JSON-LD only from a published
passport DTO and verifies that title, description, image and additional facts
come from that DTO; invented offers or prices are absent. Archived passports
remain noindex.

## Checks

- `pnpm test:seo-contract` — 6/6;
- `pnpm test:responsive-a11y-regression` — 1/1;
- `pnpm test:final-public-boundary` — 2/2;
- `pnpm test:proof-32-j` — 1/1;
- browser console — 0 errors, 0 warnings across the representative matrix.
