# TASK 44.2 — historical-file decision

Decision: `HISTORICAL_REPAIR_ALLOWED`

Checked: 2026-09-20 (Europe/Moscow)

## Evidence

TASK 44.1 established that:

- the only known Timeweb managed PostgreSQL target has no
  `public.payload_migrations` table;
- preview resolves to that same unmigrated database;
- the isolated staging/restore resource does not yet exist;
- no separate production runtime has been deployed.

Therefore no known target contains an applied checksum for either historical
file changed by PR !74.

## Exact historical scope

| File | v2 base blob | PR !74 repaired blob | Repair intent |
| --- | --- | --- | --- |
| `migrations/20260916_063247_pages_drafts_redirects.ts` | `91f6e4acabd6ceec0459dac67e0486cd246b4c90` | `10e56ad23a5d32f14d47a006508592a692740c3e` | Remove `_status` alterations that target a column absent at that point in the historical chain. |
| `migrations/20260916_231730_newbuild_schema.ts` | `29792dd2a14a4959389d7d69f49ffb6656ad37d3` | `10f6a58d4262c73343ffdb5b75971e722c5f4a3b` | Make the original developer/complex status enums compatible with the collection contract before the later collision migration. |

The new `20260919_202220_epic44_status_enum_collision` migration is forward-only
and is not itself a rewritten historical file.

## Decision boundary

The two exact historical repairs above are allowed because every known target is
unmigrated or absent. This is not a general authorization to edit applied
migrations.

Before merge, TASK 44.3 must prove clean install and the defined upgrade fixture.
If any new target evidence appears with an applied old blob/checksum, this
decision is invalidated automatically: restore the historical file and replace
the repair with a forward-only migration. No owner question is required for that
fallback.
