# TASK 50.4 — preview browser E2E

This v2 evidence is preserved for history. The approved v3 plan removed the
Turnstile dependency; current acceptance is defined by TASK 50.3A and the
canonical architecture/release checklist.

Status: `PASS`

Checked: 2026-09-20 (Europe/Moscow)

## Proven

- Local Next.js runtime started on loopback with
  `NEXT_PUBLIC_LEADS_ENABLED=false`.
- `GET /podbor/` reached HTTP `200`; the application used its public fallback
  after the intentionally unavailable dummy database timed out.
- Unit/integration proofs covered transactional save, delivery/retry/error,
  PII-free logs and retention.
- No request was sent to production, no lead was created and production data was
  not read or copied.

## Browser blocker

Chrome DevTools navigation to the loopback page was attempted twice. Both runs
timed out at the browser daemon boundary and ended at `chrome-error://chromewebdata/`
although the Next.js log recorded HTTP `200`. Repeating the same browser action
was stopped.

The local PostgreSQL 18 service is loopback-only but has no discoverable
project-owned credentials for this worktree. Reusing another project's role or
changing the shared server authentication was not attempted.

## Resume condition

Resume browser E2E when either:

1. EPIC 49 provides the isolated preview runtime; or
2. a project-owned local PostgreSQL role/database is available.

Current follow-up must prove disabled and enabled form states plus local
submit/Admin/duplicate/retry/error E2E. It is not conditional on an external
CAPTCHA account or keys.

## Resumed preview proof

Checked: 2026-09-21 (Europe/Moscow) on exact preview release
`f06fdb06c1e42aff3a117c4b7a2f22b74455708d`.

- Real Chrome/CDP opened `https://more-previu.tw1.ru/podbor/` in an isolated
  browser context.
- The form exposed `data-leads-enabled="false"` before interaction.
- Valid name, phone, task and consent values were entered and the submit button
  was activated.
- The form moved to `data-state="server-disabled"` and displayed the approved
  warning that sending remains gated by Leads API and legal-text approval.
- Chrome network evidence contained no `POST /api/public/leads` request.
- The single managed database contained zero `leads` rows both before and after
  the interaction.

The runtime-contour blocker was therefore resolved. No external captcha or
Cloudflare dependency was used.

## Enabled preview E2E

Checked: 2026-09-21 (Europe/Moscow) on the technical preview only.

The leads-enabled build was installed temporarily from exact branch release
`17db71b113fe6ca3e946ecfbba003f7bd7705c45`. External notification channels
remained disabled.

- malformed JSON shape returned `400` and created no row;
- the honeypot path returned `400` and created no row;
- a form submitted faster than the minimum fill time returned `400` and
  created no row;
- a real browser form submit returned `201`, moved the form to `success` and
  created exactly one lead;
- the fifth request in the window remained a validation `400`; the sixth
  returned rate-limit `429`;
- the stored row had `new` status, `/podbor/` source path, accepted consent with
  version and timestamp, active retention status and zero delivery rows;
- an authenticated Payload owner could read the same row through the protected
  API.

The first Admin UI render exposed an existing generated import-map drift:
`@payloadcms/storage-s3/client#S3ClientUploadHandler` was absent. The generated
map was repaired in exact release
`46484177f03743b3e61183700b3cda591b54c5e1`. After that release was installed,
the Payload Leads screen rendered normally and showed the single test lead as
`New`.

Application logs contained one accepted event and five rejected events with
technical reasons only. The synthetic name, phone and message had zero log
matches.

## Cleanup and restored state

- The synthetic lead was deleted through the authenticated Payload API after
  the Admin proof.
- Database cleanup proof: zero matching test leads and zero lead deliveries.
- The preview was rolled back to the approved application release
  `f06fdb06c1e42aff3a117c4b7a2f22b74455708d`.
- `NEXT_PUBLIC_LEADS_ENABLED=false` was restored.
- Loopback health passed after rollback.
- A final valid browser interaction moved the form to `server-disabled`, showed
  the approved warning and emitted no `POST /api/public/leads` request.

TASK 50.4 acceptance is complete: one valid lead was saved and visible to the
operator; invalid, honeypot and rate-limit flows created no extra rows; consent,
delivery state, PII-free logging and final cleanup were proven.
