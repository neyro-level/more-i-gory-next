# TASK 44.3 — verification evidence

Date: 2026-09-20  
Branch: `codex/epic44-db-migration-repair`  
Runtime: local native PostgreSQL 18.6 on loopback port 5435  
Production impact: none

## Clean install

A fresh disposable database received the complete registered Payload migration
chain. All 26 migrations completed in order, including the repaired historical
files and `20260919_202220_epic44_status_enum_collision`.

Post-run evidence:

- `payload_migrations`: 26 rows;
- public schema: 148 tables;
- `verify:schema`: PASS with no generated schema drift.

## Upgrade fixture

A second disposable database first received the same complete 26-migration
chain. The final status-split migration was then reversed with its exact
checked-in `down` statements and removed from the disposable migration ledger,
creating the 25-migration predecessor state without changing repository files.

Fixtures covered publication statuses `published`, `hidden` and `archived` in
developers and complexes, plus Payload draft state in the separate `_status`
and version `_status` columns. Reapplying the registered final migration passed.

Post-run evidence:

- `payload_migrations`: 26 rows;
- developer publication statuses preserved: `archived,hidden,published`;
- complex publication statuses preserved: `archived,hidden,published`;
- Payload draft states remained separate and preserved;
- developer and complex version rows remained `published/draft`.

An earlier harness attempt used `payload migrate:down`, which rolls back an
entire batch rather than one migration. It reached an unrelated legacy down
path and failed on a constraint name. That disposable database was discarded;
the command is not part of the accepted rehearsal and no production or managed
database was touched.

## Regression and static gates

| Check | Result |
|---|---|
| `pnpm test:pages-drafts-migration` | PASS, 3/3 |
| `pnpm test:newbuild-schema-contract` | PASS, 7/7 |
| `pnpm test:property-enums` | PASS, 5/5 |
| `pnpm verify:schema` | PASS |
| `pnpm typecheck` | PASS |
| `pnpm lint` | PASS, 0 errors; 3 pre-existing warnings outside this diff |

SourceCraft run `107` correctly failed closed because the new test command was
not yet registered in `verify:quick`. The alias was added to the canonical
suite before the replacement exact-head gate; run `107` is not accepted as
delivery evidence.

Both disposable databases were deleted after verification. Exact-head review,
the single SourceCraft RISKY gate and merge are recorded by the terminal
delivery ledger; any push after that gate invalidates it.
