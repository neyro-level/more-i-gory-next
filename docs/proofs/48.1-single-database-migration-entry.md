# TASK 48.1 — Single-database migration entry

Date: 2026-09-21 (Europe/Moscow)
Mode: read-only managed-database preflight; no DDL, seed, dump or restore

## Task contract

- Goal: prove that the one approved managed database is eligible for the
  committed migration chain and freeze the exact input for TASK 48.O.
- Data owner: Payload `3.89.0`; PostgreSQL adapter with `push:false`.
- Boundary: cluster `4210557`, database `default_db`, PostgreSQL 18.
- Non-goals: a second database or user, a local/managed restore target,
  production data, speculative DDL, seed or production rollout.
- Gate: `RISKY`; managed mutation is allowed only by TASK 48.O from the exact
  merged TASK 48.P runbook SHA.

## Read-only target proof

The query ran from the approved preview server consumer contour without
printing credentials or row payloads.

| Check | Result |
| --- | --- |
| Database identity | `default_db` |
| PostgreSQL version | `18.6` |
| Public tables | `0` |
| `public.payload_migrations` | absent |
| Unknown migration history or application rows | not detected |

The v3 stop condition is therefore clear: the target is empty/unmigrated and
eligible for the committed chain. If a later preflight observes any public
table, migration row or unexpected application data, TASK 48.O must stop before
DDL and record the changed state.

## Exact repository chain

- Exact base SHA: `7503ea2ddbdd68de917bf8ca4366284abc1c4382`.
- Registered migrations: `26`; TypeScript migration files: `26`.
- Unique registered names: `26`; missing files: `0`; duplicate names: `0`.
- First: `20260915_171809_initial_users_jobs`.
- Last: `20260919_202220_epic44_status_enum_collision`.
- `migrations/index.ts` SHA-256:
  `6b1cbf62ba9e02ad49f12165f3c9142faf656a15ea5a78ad64b4e5eb56450a07`.

Relevant migration contract tests passed on the exact checkout:

- pages/drafts/status collision: `3/3`;
- deactivation approval and clean/upgrade registration: `2/2`;
- lead delivery registration: `1/1` applicable (`1` DB-dependent case skipped
  because this task deliberately did not pass a database credential locally);
- property enums: `5/5`;
- property numeric contract: `1/1`;
- canonical import-run status and ordering: `3/3`.

TASK 48.1 also adds a fail-closed operator at
`ops/runtime/single-db-migration.mjs`. Its default mode is read-only preflight;
apply requires the full expected Git SHA and the explicit
`APPLY_SINGLE_DB_MIGRATIONS` confirmation. It rejects a dirty checkout, another
database or PostgreSQL major, any existing public table, any migration history,
ambiguous query output, and a post-apply ledger/table mismatch. Credentials are
passed to `psql` through its process environment and are never printed or put
in command arguments.

Focused operator tests passed `6/6`; the existing newbuild schema contract
passed `7/7`. Project lint completed with `0` errors and the same `3` unrelated
warnings already present in the baseline.

## Operational handoff

TASK 48.1 does not mutate the managed database. TASK 48.P must first merge the
single-database preflight/migration/seed/smoke runbook through an exact-head
RISKY gate. TASK 48.O then repeats the read-only identity and emptiness check
and may apply this exact committed chain. Restore proof remains
`DEFERRED_BY_OWNER_DECISION` under `V3-OD-02`; no replacement proof is claimed.

## Result

`PASS`: the only approved database is unambiguously empty/unmigrated and the
exact 26-migration repository chain is internally complete. The later managed
mutation has a fail-closed entry condition and no second database is required.
