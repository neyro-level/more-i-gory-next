# TASK 47.1 — Rotation preflight

Date: 2026-09-20 (Europe/Moscow)  
Mode: read-only; no provider, Secret Master, server or runtime mutation

## Verified topology

- Timeweb Managed PostgreSQL cluster: `4210557`.
- Runtime state: `started`; engine: `postgres18`.
- Active preset: `357`, `1 CPU / 1 GB RAM / 8 GB NVMe`.
- Timeweb API returns exactly one database administrator.
- The administrator login matches `MOREIGORY_DATABASE_USER` in Secret Master;
  neither value nor password was printed or recorded.
- Official Timeweb documentation states that the minimum PostgreSQL preset
  permits only one database and one user. A parallel replacement identity is
  therefore unavailable without changing the owner-approved tariff.

Official provider evidence:

- <https://timeweb.cloud/docs/dbaas/dbaas-create>
- <https://timeweb.cloud/docs/dbaas/dbaas-manage/users>

## Approved in-place sequence

The operational task must run only from the exact merged EPIC 47 runbook SHA.

1. Confirm exact cluster `4210557`, the single administrator identity, healthy
   preview service, and a working database connection.
2. Generate a strong replacement password in process; never print it or write it
   to repository files, command history or evidence.
3. Change the password of the existing administrator through the Timeweb API.
   This starts the short controlled preview maintenance window.
4. Update the canonical Secret Master value
   `more-i-gory-server/prod/MOREIGORY_DATABASE_URL` and its password component.
   Do not create a second database URL, user or staging credential.
5. Render `/etc/moreigory/app.env` from the canonical secret route, keep only
   `DATABASE_URI`, and normalize the file to LF. Remove the legacy duplicate
   `DATABASE_URL` and stale CAPTCHA/second-DB variables if present.
6. Validate the environment contract without printing values, then perform one
   controlled preview service restart.
7. Prove `/api/health` and a real database consumer query. Confirm the previous
   password is rejected using an in-memory connection attempt.
8. Record only redacted names, counts, timestamps, exact SHA and PASS/FAIL.

## Recovery and stop conditions

- Before step 3: stop without mutation on any identity, target or permission
  ambiguity.
- After step 3: do not restore an unrecorded old secret. Complete the forward-fix
  with the new credential; if necessary, set another new password at the exact
  same provider identity and repeat the secret/env update.
- Stop before restart if Secret Master or `/etc/moreigory/app.env` cannot be
  updated exactly and atomically.
- Stop and retain preview maintenance if health or database smoke fails; do not
  touch `moreigori.ru`, DNS, TLS or any production rollout.

## Result

`PASS`: the minimum tariff is confirmed as a one-user topology, and the required
provider → Secret Master → app env → controlled restart → health/DB smoke order
is unambiguous. No secret value was exposed and no external state was changed.
