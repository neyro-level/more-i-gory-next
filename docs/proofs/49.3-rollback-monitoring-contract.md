# TASK 49.3 — rollback and monitoring contract

**Status:** CONTRACT_READY / LOCAL PASS

- rollback accepts only the loopback health endpoint;
- `current` switches by atomic symlink rename;
- successful restart and health smoke swaps `current`/`previous`;
- failed smoke restores the original `current` pointer and restarts it;
- no database down, restore or schema rollback exists in this path;
- a local systemd timer checks health every five minutes without external SaaS;
- application, health timer and Nginx log locations are fixed in the runbook.

Executable Linux fixtures prove both the successful swap and failed-smoke
recovery. Live symlink switching, service restart and timer installation remain
TASK 49.O after TASK 49.P merge.
