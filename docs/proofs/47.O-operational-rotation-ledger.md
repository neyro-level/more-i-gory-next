# TASK 47.O — Operational rotation ledger

Date: 2026-09-21 (Europe/Moscow)
Mode: controlled preview maintenance from merged SHA
`2d7c043bfc4852dca85511dccaec8d77afcfb59c`; values and credentials were not
printed or recorded

## Exact targets

- Timeweb Managed PostgreSQL cluster: `4210557`, PostgreSQL 18.
- Existing administrator: exact ID `458023`; no second database or user created.
- Secret Master: `more-i-gory-server/prod`, path `/`.
- Runtime: host `moregory1`, `/etc/moreigory/app.env`,
  `moreigory.service`.
- Public proof: `https://more-previu.tw1.ru/api/health`.

## Execution evidence

1. Secret Master machine access was verified by a changed-value canary:
   create, update and delete all returned `PASS`.
2. The existing Timeweb administrator password was rotated in place.
3. `MOREIGORY_DATABASE_PASSWORD` and `MOREIGORY_DATABASE_URL` were updated in
   Secret Master before runtime delivery.
4. Existing Payload and S3 values were preserved from the live runtime and
   imported into the same Secret Master scope. `REVALIDATE_SECRET` was created.
5. Legacy `DATABASE_URL` and all `POSTGRESQL_*` duplicates were removed. No
   CAPTCHA, Turnstile or second-database secret was created.
6. `/etc/moreigory/app.env` was atomically replaced as UTF-8/LF with exactly
   the 17 keys required by `ops/runtime/validate-preview-env.mjs`.
7. File ownership and mode are `root:deploy:640`; `moreigory.service` was
   restarted and is active.
8. Database consumer smoke from the application server passed with the new
   credential. The previous credential was rejected.
9. Public health returned HTTP `200` with `X-Robots-Tag: noindex, nofollow`.

## Result

`PASS`: the single approved database credential is current, its predecessor is
revoked, Secret Master is canonical, the preview env contract is complete, and
the preview consumer is healthy. The final domain, DNS, TLS and production
rollout were not changed.
