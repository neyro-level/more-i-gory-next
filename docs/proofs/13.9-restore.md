# TASK 13.9 — Backup / restore proof

**Дата:** 2026-09-19  
**SHA:** pending commit on `work/epic-13`  
**Секреты:** не включены.

## Контракт

```text
backup
→ restore into disposable/staging DB
→ app reads restored state
```

Команда: `ops/runtime/backup-restore.sh`  
Тест: `pnpm test:backup-restore` — PASS.

## Live

Попытка на preview-хосте: FAIL. Роль приложения не может создать disposable database (`permission denied to create database` / authentication при неэкранированном env). Production dump не выполнялся.

Нужен owner: отдельная admin-роль или заранее созданные `moreigory_staging` / `moreigory_restore_dst` в Timeweb Managed PostgreSQL.

Provider backup Timeweb остаётся включённым на стороне кластера; это не заменяет restore evidence.
