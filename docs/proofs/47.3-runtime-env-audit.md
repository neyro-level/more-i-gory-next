# TASK 47.3 — Runtime env completeness audit

Date: 2026-09-20 (Europe/Moscow)  
Mode: read-only; values and credentials were not printed or recorded

## Live preview facts

- `moreigory.service`: active.
- `/etc/moreigory/app.env`: present; no duplicate keys.
- `NEXT_PUBLIC_SERVER_URL`: matches `https://more-previu.tw1.ru`.
- Database route: `DATABASE_URI` is present and has PostgreSQL URL shape.
- `PAYLOAD_SECRET`: present and meets the minimum length contract.
- S3: endpoint, region, bucket, access key, secret key and
  `S3_MEDIA_PREFIX=staging/media` are present.
- Runtime contour flags are complete across the env file and systemd unit:
  `AMS_PROFILE=REALTY_BASE`, `TZ=Europe/Moscow`,
  `AMS_RUNTIME_CONTOUR=staging`, `JOBS_AUTORUN=false`, and
  `NEXT_PUBLIC_LEADS_ENABLED=false`.
- No Turnstile/CAPTCHA key and no `MOREIGORY_STAGING_DATABASE_URL` exists.
- The previously documented legacy `DATABASE_URL` duplicate is not present in
  the current live file; current live truth supersedes that earlier observation.

## Drift that TASK 47.P / 47.O must close

- The file contains mixed line endings (`5` CRLF sequences); the operational
  render must rewrite it atomically as UTF-8 with LF only.
- HTTP cache revalidation is not configured:
  `CACHE_INVALIDATION_MODE`, `REVALIDATE_SECRET`, and
  `INTERNAL_REVALIDATE_BASE_URL` are absent. CMS mutation hooks use the HTTP
  invalidator, so the preview contract requires these three values.
- Secret Master currently holds the canonical database/server access material,
  while the existing Payload and S3 runtime secrets live only in the server env.
  Before rendering, TASK 47.O must safely preserve/import the existing values
  into the exact project scope or otherwise prove a single recoverable source;
  values must never pass through logs or repository files.

## Required target contract

```text
AMS_PROFILE=REALTY_BASE
TZ=Europe/Moscow
AMS_RUNTIME_CONTOUR=staging
JOBS_AUTORUN=false
NEXT_PUBLIC_LEADS_ENABLED=false
DATABASE_URI=<canonical MOREIGORY_DATABASE_URL>
PAYLOAD_SECRET=<non-empty, at least 32 characters>
NEXT_PUBLIC_SERVER_URL=https://more-previu.tw1.ru
CACHE_INVALIDATION_MODE=http
REVALIDATE_SECRET=<non-empty, at least 32 characters>
INTERNAL_REVALIDATE_BASE_URL=http://127.0.0.1:3000
S3_ENDPOINT=https://s3.twcstorage.ru
S3_REGION=ru-1
S3_BUCKET=moreigory-media
S3_ACCESS_KEY=<non-empty>
S3_SECRET_KEY=<non-empty>
S3_MEDIA_PREFIX=staging/media
```

Forbidden after render: `DATABASE_URL`, `MOREIGORY_STAGING_DATABASE_URL`, any
`TURNSTILE_*`/`CAPTCHA_*` key, production contour, enabled jobs or enabled public
leads.

## Result

`PASS WITH DECLARED REMEDIATION`: database, Payload, preview URL, S3 and contour
facts are unambiguous; the exact LF and revalidation drift is known and bounded.
No external mutation is allowed until the runbook/tests pass a RISKY gate and
are merged. TASK 47.O owns the atomic env update, restart and consumer smoke.
