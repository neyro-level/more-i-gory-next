# TASK 46.2 — existing database truth

**Checked:** 2026-09-20 (Europe/Moscow)

**Mode:** read-only. No database, user, cluster, schema or row mutation was
performed. Secret values and connection strings were not printed or written to
the repository.

## Canonical identity

| Check | Result |
|---|---|
| Secret Master | `more-i-gory-server/prod` readable; database, SSH and Timeweb API secret names present |
| Timeweb cluster | exactly one cluster `4210557`, `More-gory BD`, `postgres18`, status `started` |
| Project / preset | project `2932741`; preset `357` |
| Current preset | 1 CPU, 1 GB RAM, 8 GB disk, `ru-1`, 496 RUB/month |
| Database topology | exactly one instance: `default_db`; exactly one admin identity |
| Consumer connection | PASS through the project server and server-side runtime env |
| Engine | PostgreSQL `18.6` |
| Data baseline | `0` tables in the current public schema; no production application rows detected |

This is the single owner-approved data contour for preview and the future
release. The smallest current tariff is sufficient because v3 explicitly
excludes a second database, a second user and a disposable restore target.

## Access boundary

- Timeweb evidence came from authenticated read-only `GET` requests.
- Database evidence used the deploy SSH route plus non-interactive scoped sudo
  to read the server-side env and run aggregate metadata queries only.
- Direct root SSH was unavailable and is not required; the canonical deploy
  route remains usable.
- The server env file contains CRLF line endings, so a shell-sourced URI needed
  in-process carriage-return trimming for this smoke. TASK 47 must normalize the
  runtime env file before relying on shell sourcing; no secret value is affected
  in repository evidence.

## Decision

No new Timeweb resource is required or permitted by v3. The former staging and
restore quote is historical only and must not block the remaining graph.
