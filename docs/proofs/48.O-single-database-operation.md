# TASK 48.O — single-database operational ledger

**Executed:** 2026-09-21 (Europe/Moscow)  
**Database:** existing Timeweb Managed PostgreSQL `default_db`, PostgreSQL 18  
**Final application proof:** exact SourceCraft `main`
`e437099e328eb709e6f3ec6cefe22d8513595a48`

## Result

`PASS`: the one approved database has the complete committed migration chain,
one technical owner and two minimal proof records. Payload Admin/auth, the
public list/detail boundary, unpublished `404` and persistence across a full
exact-code runtime restart passed. Production, DNS, TLS and the public preview
artifact were not changed.

## Database evidence

- Preflight before DDL: database `default_db`, PostgreSQL major `18`, `0`
  public tables, no migration ledger and no unknown application rows.
- Applied exactly the committed migration chain once from clean merged source.
- Final migration ledger: `26` rows; final public schema: `148` tables.
- Final redacted counts: `1` user, `1` owner, `2` properties.
- Proof rows: one active record with `publishedAt` and `verifiedAt`; one active
  draft boundary record without `publishedAt`.
- No schema push, raw manual DDL, downgrade, dump, restore, second database or
  second database user was used.

## Application evidence

- Owner bootstrap is idempotent: the final repeat returned `exists`.
- Minimal seed completed with `verdict=PASS` and created the published and
  draft technical records.
- Exact-main smoke before restart: health, Payload Admin, owner login, public
  list, published detail and unpublished `404` all `PASS`.
- The exact-code process was fully stopped and started again against the same
  database through a loopback-only runtime and private SSH database tunnel.
- Exact-main smoke after restart returned the same six `PASS` results.
- Loopback proof intentionally reports proxy `noindex` as
  `NOT_APPLICABLE_LOOPBACK`; public Nginx/noindex and immutable artifact
  installation belong to EPIC 49.
- The existing public preview service remained active on its previous artifact;
  this task did not perform a staging artifact deploy.

## Forward fixes discovered by the operation

The operation stopped safely before each failed step and merged only reviewed
forward fixes:

- PR 86 / gate 125: execute owner bootstrap and seed through Payload CLI.
- PR 88 / gate 127: add the required React server condition; conflicted PR 87
  was superseded and never merged.
- PR 89 / gate 128: remove duplicate Drizzle numeric columns by preserving
  Payload camelCase field keys and explicit SQL names.
- PR 90 / gate 129 and PR 91 / gate 130: align the smoke with trailing-slash
  routing and separate loopback application proof from Nginx noindex proof.
- PR 92 / gate 131: query nested Payload array paths (`sources.label` and
  `facts.label`) instead of unsupported top-level array paths.

Final exact-main application SHA: `e437099e328eb709e6f3ec6cefe22d8513595a48`.

## Credential safety

Technical owner credentials remain only in Secret Master
`more-i-gory-server/prod`. The owner password was rotated after the operation;
the new credential passed login, the previous credential was rejected, and the
Secret Master value was verified without recording either value.

## Restore boundary and recovery

Restore proof is `DEFERRED_BY_OWNER_DECISION` under approved v3 decision
`V3-OD-02`. No replacement simulation is claimed. After the committed DDL,
recovery is forward-fix only: keep preview isolated, diagnose the exact failed
step and finish the committed chain. A production disaster-recovery rehearsal
requires a separate owner decision and is outside this plan.
