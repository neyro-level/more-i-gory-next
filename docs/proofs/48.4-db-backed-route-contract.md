# TASK 48.4 — DB-backed route contract

Date: 2026-09-21 (Europe/Moscow)
Mode: code/test delivery; live DB-backed execution remains TASK 48.O

## Delivered contract

- `/obekty/` now always reads published manual properties through the Public
  Gateway, including on the technical preview contour. Editorial preview still
  adds technical navigation and templates but no longer hides valid DB rows.
- Detail reads continue through `getManualPropertyRouteBySlug`; incomplete
  manual drafts do not satisfy the publication predicate and return `404`.
- Raw anonymous Payload REST remains denied. Public pages receive selected DTOs
  through Local API with explicit access mode.
- Payload remains the only schema owner and keeps `push:false` plus committed
  migrations.

`ops/runtime/single-db-application-smoke.mjs` is pinned to
`https://more-previu.tw1.ru` and verifies:

1. health and preview `noindex, nofollow`;
2. Payload Admin is reachable;
3. owner login succeeds using credentials supplied only through process env;
4. the published technical proof record appears in list and detail HTML;
5. the unpublished proof slug returns `404`;
6. the same checks run in explicit `before-restart` and `after-restart` phases.

The smoke prints only redacted PASS fields. It does not print login responses,
credentials or row payloads, and it refuses the final production origin.

## Checks

- application smoke contract: `4/4`;
- properties public routing and publication boundary: `7/7`;
- anonymous REST boundary: `5/5`;
- explicit Local API access mode: `1/1`.

## Operational boundary

No managed migration, owner creation, seed or service restart was executed in
this task. TASK 48.P must merge the exact runbook/code first. TASK 48.O then
creates only the approved minimal technical records in the one database, runs
the smoke before and after a controlled preview restart, and records evidence.

## Result

`PASS`: the application path and fail-closed smoke needed for DB-backed live
proof are implemented without entering production or mutating the managed DB.
